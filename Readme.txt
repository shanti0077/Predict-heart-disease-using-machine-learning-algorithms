#To start the streamlit app follow below instructions:

1.locate the both file "final_model_KNN.pkl" and "Heart_disease.py" in command prompt using cd command. 
2.run command "streamlit run ./Heart_disease.py", then app will deploy in same network machines.